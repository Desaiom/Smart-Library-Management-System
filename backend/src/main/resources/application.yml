# =====================================================================
# Base configuration (shared by all profiles).
# Active profile is chosen via SPRING_PROFILES_ACTIVE or the value below.
# =====================================================================
spring:
  application:
    name: smart-library
  profiles:
    active: dev
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: omdesai0305@gmail.com
    password: zdyv psit liwj nmnz

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

cloudinary:
  cloud-name: dkddidsq8
  api-key: 116326479536312
  api-secret: CLfaGRhBkrj65hlsVF2sYNelSKw

server:
  port: 8080
  error:
    include-message: always

# Custom application properties, bound by @ConfigurationProperties(prefix = "library")
library:
  jwt:
    secret: "change-me-in-prod-use-a-64-byte-base64-secret-0123456789abcdef0123456789abcdef"
    expiration-ms: 86400000        # 24 hours
  borrow:
    loan-period-days: 14
    fine-per-day: 5.0
    max-active-borrows: 5
  storage:
    upload-dir: uploads

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

management:
  endpoints:
    web:
      exposure:
        include: health,info
